/* import { ComponentFixture, TestBed } from '@angular/core/testing'

import { DetalleFiguritaComponent } from './detalle-figurita.component'

describe('DetalleFiguritaComponent', () => {
  let component: DetalleFiguritaComponent
  let fixture: ComponentFixture<DetalleFiguritaComponent>

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [DetalleFiguritaComponent]
    })
    fixture = TestBed.createComponent(DetalleFiguritaComponent)
    component = fixture.componentInstance
    fixture.detectChanges()
  })

  it('should create', () => {
    expect(component).toBeTruthy()
  })
})
 */